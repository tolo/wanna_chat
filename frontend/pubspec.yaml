name: wanna_chat_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.18.0
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  flutter_markdown: ^0.6.22

  result_notifier: ^0.4.1

  shared_model:
    path: ../shared_model

dev_dependencies:
  flutter_test:
    sdk: flutter

  lint: ^2.3.0

flutter:
  uses-material-design: true

  assets:
    - assets/
